# Fill in your real key below, then save. That's it — no copy step needed.
# This file is gitignored, so your key won't leak if you put the folder under git.

# Google AI Studio key for Gemini 2.5 Flash Image (asset generation).
# Used only by scripts/gen_image.py. The agent (Claude/Cursor/etc.) uses its
# own vision + writing — no separate Anthropic key needed here.
# Get one at https://aistudio.google.com/apikey  (free tier is enough)
GEMINI_API_KEY= 这里填写Key！
